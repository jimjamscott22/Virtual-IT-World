# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A single-user helpdesk simulator (`vitsc` — Virtual IT Support Center), built as personal practice for desktop-support/helpdesk job applications. The player works tickets against a simulated Windows/AD environment (`Meridian Freight Co.`) using tools that mirror real utilities (AD console, PowerShell, network tools, event viewer, print management, remote session).

Full context lives in:
- Design spec: `docs/superpowers/specs/2026-08-07-virtual-it-support-center-design.md`
- Phase 1 plan: `docs/superpowers/plans/2026-08-07-phase-1-drill.md`
- Phase 2a plan: `docs/superpowers/plans/2026-08-14-phase-2a-depth-mechanics.md`
- **Current state, next task, and open threads: `docs/handoff.md`** — read this
  first if you are picking the work up mid-stream.

Phase 1 is complete (Tasks 1–19): the world model, `SimulatedEnvironment`, the full v1 fault catalog (all ten faults across identity/network/printing/endpoint), the six player-facing tools, the whole persona layer (`PersonaCard`, the `Persona` protocol, `TemplatePersona`, and the LM Studio client with its leak filter), the session layer (`Ticket`, priority/SLA, `SessionQueue`, grading, and the after-action report), SQLite persistence for closed tickets (`session/store.py`: `Store`, `ClosedRecord`, `DomainStat`), the full FastAPI + HTMX web app (`web/app.py`, `web/deps.py:AppSession`, `web/routes/queue.py`, `web/routes/tools.py`, `web/routes/chat.py`, `web/routes/close.py`, `web/routes/events.py`; `uv run python -m vitsc` to run it), and end-to-end coverage (`tests/test_end_to_end.py`) proving a full ticket can be worked start to finish through the HTTP surface alone, across every fault in the catalog. What Phase 1 deliberately did not include: `AppSession` built with `TemplatePersona` only — that is now resolved by Phase 2a Tasks 1–2 (below). Still outstanding from Phase 1's Definition of Done: the model-backed path (`uv run pytest` green *with* LM Studio running) has never been exercised, because no environment used so far has network access to a local LM Studio instance. That half is unverified, not failing, and `docs/verifying-lmstudio.md` is the manual procedure for it. Phase 2a Tasks 1–3 have landed; see `docs/handoff.md` for exactly where the work stopped.

Phase 2a is now underway against `docs/superpowers/plans/2026-08-14-phase-2a-depth-mechanics.md`. **Task 1 has landed**: leak terms bind per *ticket* rather than at construction, which was the Phase 1 blocker on a model-backed persona (a session runs many faults in turn, but `LMStudioPersona.__init__` took one fixed `leak_terms` list). The `Persona` protocol gained `for_fault(leak_terms) -> Persona` — `TemplatePersona` returns itself (it only ever repeats symptom fields, so it has no generated vocabulary to filter), `LMStudioPersona` returns a copy carrying the new terms while sharing the client, model, fallback and degraded flag by reference. Binding is resolved by `SessionQueue.persona_for(ticket)`, so `web/routes/chat.py` speaks to a user without importing the fault registry. Leak terms still reach `scrub()` only and never a prompt — naming the forbidden word to the model hands it the answer.

**Task 2 has landed**: the model-backed persona is now reachable from the app. `persona/config.py` holds `PersonaSettings.from_env()` (`VITSC_PERSONA`, `VITSC_BASE_URL`, `VITSC_MODEL`) and `build_persona()`; `AppSession.build` calls them when no explicit `persona` argument is passed, so the whole test suite still injects `TemplatePersona` directly. The default stays `template` and an *unrecognised* backend falls back to it rather than raising — a typo in a shell variable should cost you the model persona, not the drill. `AppSession.degraded` reads through to `queue.persona`, and the amber banner in `layout.html` is driven from the SSE payload rather than the initial render, because degradation starts mid-session. `docs/verifying-lmstudio.md` is the manual procedure for the half of the Definition of Done that no pipeline here can check.

**Task 3 has landed**: the `Distractor` protocol (`distractors/base.py`), its registry (`distractors/registry.py`), and the conformance harness (`tests/test_distractors.py`). A distractor is a real, truthfully-reported anomaly that is never the cause of a ticket, so it deliberately has *no* `is_present`, `symptoms`, `canonical_resolutions` or `escalation_is_correct` — a `Fault` carrying an `is_ticketable = False` flag would have to be excluded from the scheduler, the fault harness, priority, grading and the after-action, five places that could each forget. Its whole contract is the non-interference guarantee, and the harness enforces it mechanically: a distractor may never flip any registered fault, must be visible through at least one query a clean world answers differently, must leave invariants clean, and must not break a canonical repair. `tests/test_architecture.py` also now forbids any module under `tools/` importing `vitsc.distractors`, for the same reason it forbids `vitsc.faults`: a tool that could tell a decoy from a cause would make the seeded noise dishonest.

**Task 4 has landed**: `distractors/catalog.py` now carries five distractors — a moderately low disk (`disk.moderately_low`, 8–15 GB against `endpoint.disk_full`'s 2.0 GB threshold), a stopped `WSearch` service, a month-old disk warning in the event log, an offline printer nobody has installed (its `placements()` legitimately returns `[]` against today's estate, where every seeded printer is installed somewhere — it starts applying once Phase 2b grows the estate), and a stale `Z:` drive mapping. `session/queue.py:seed_distractors(world, rng, count)` applies `count` distinct ones and `SessionQueue.__init__` calls it *before* `capture_baseline`, storing the result on `self.distractors` — the same capture-after-apply ordering `world/invariants.py` already documents for faults, so seeded noise is inherited world state rather than the technician's own collateral damage. `distractor_count` defaults to 0, so every existing test stays deterministic; `AppSession.build` passes 3 so a real session always has noise. `tests/test_distractor_registry.py`'s empty-catalog assertion is now `test_the_catalog_is_not_empty`, flipped the moment the catalog stopped being empty.

**Task 5 has landed**: cascades — the mechanism for one fault instance producing several tickets, one per affected person. `faults/base.py` gained `FaultBase` (defaults for `kb_articles`, `escalation_reason`, `escalation_evidence`, and `reporters()`, which returns `None` — "whoever the placement points at" — unless a fault overrides it with an explicit list of `sam`s); all ten catalog faults now inherit it, unchanged otherwise. `session/queue.py:resolved_reporters(world, fault, at)` resolves that to a concrete list (falling back to the single-reporter `reporter_sam` when a fault declares none, dropping anyone missing from `world.org.users`), and `_candidates()` filters on it being non-empty rather than on `reporter_sam(...) is not None`. `SessionQueue.open_ticket()` now returns `list[Ticket]`: it applies the chosen fault once, then mints one ticket per `rng.sample`d reporter, capped by `CASCADE_MAX = 3` and by whatever room is left under `MAX_ACTIVE`, sharing one `cascade_id`. A candidate whose full cascade would not fit in the room left is skipped entirely rather than dealt partially. `open_one()` returns the first ticket (or `None`) for every caller that only ever wanted a single arrival, and `open_cascade(fault)` opens a named fault's cascade directly for tests. `Ticket.cascade_id: str | None` and `priority_for(fault, user, reporters=1)` (P1 at three or more reporters, P2 above one) round it out. No fault yet declares an explicit `reporters()` list — Task 7's `print.server_spooler_stopped` is the first — so every existing session still deals exactly one ticket per call; `tests/test_cascade.py`'s two tests that name that fault directly are `xfail(raises=KeyError, strict=True)` until then.

**Task 6 has landed**: cascade-aware grading and reporting, on top of Task 5's data model. `grading.py:duplicate_mutations(ticket, siblings=None)` counts repeated identical mutating `(tool, command, args)` calls beyond their first use, folding in every other ticket the same cascade produced when `siblings` is given — the case this exists for is a technician who re-runs the same fix once per sibling ticket rather than once for the shared root cause. `Grade` gained a `duplicate_mutations` field, and `grade_ticket()` gained an optional `siblings` parameter that feeds it — the parameter never touches `correct`/`fault_cleared`/`disposition_correct`: `is_present()` against the live world already covers every sibling on its own, so branching pass/fail on `siblings` would be exactly the fault-aware special-casing the core design principle forbids. `afteraction.py:build_after_action()` gained the matching `siblings` parameter and `AfterAction.cascade_note` — empty for a single ticket, otherwise naming the shared cause and the sibling count (`"One <fault> was behind 3 tickets — the fix was a single fix."`) — plus a verdict suffix (`"You fixed this 3 times. One root cause needs one fix."`) appended only when the fault cleared and `duplicate_mutations > 0`. `store.py` persists `cascade_id` on `ClosedRecord` and the `closed_tickets` schema; `Store.init()` guards the new column behind a `PRAGMA table_info` check with `ALTER TABLE ... ADD COLUMN`, so a database created before this task still opens. `web/routes/close.py` resolves a closing ticket's siblings from `session.queue.tickets` by matching `cascade_id` and passes them through to both calls; `_afteraction.html` renders `cascade_note` when present. Verified against a throwaway multi-reporter stub fault (registered, exercised, discarded), the same technique Task 5 used, since no real fault exercises the cascade path until Task 7.

**Task 7 has landed**: `print.server_spooler_stopped` (`faults/catalog/printing.py`), the reference cascade fault Tasks 5 and 6 were built against without one existing yet — the first fault to override `FaultBase.reporters()` with an actual list instead of `None`. Its placement is a print server (`assigned_to is None` and hosting at least one printer — `MER-PRT-01` today, via `_print_servers(world)`), not a workstation, which is why it needs its own placements helper alongside `SpoolerStopped`'s `_workstations_with_printers`. `reporters()` returns every user whose assigned machine has a printer installed whose `host` is this server, sorted for determinism — six people today, capped to `CASCADE_MAX` by the queue. Its `diagnostic_path()` cannot resolve a printer name from `World` (the method receives only a `Placement`), so its second query hardcodes a literal printer hosted on that server (`_SERVER_DIAGNOSTIC_PRINTER = "PRT-ACC-01"`) the same way `net.static_dns_misconfig`'s diagnostic already hardcodes `MER-FS-01` — `printer.state`'s rendered `SpoolerState` reads the `from` machine's own service state, so any printer on the server works. `symptoms().scope` — "a couple of people near me said the same" — is the honest, jargon-free cascade tell, mirroring how `account_locked`/`password_expired` differ by whom they affect rather than by mechanism. `web/templates/_queue.html` renders `ticket.cascade_id` as a small tag on each sibling row; the rows themselves stay independent and unmerged on purpose, so the technician has to notice the pattern rather than have it pre-solved. `tests/test_catalog.py:test_v1_catalog_is_complete` — despite its name, the "currently complete" catalog check — now includes the new id. Landing this fault is also what flips Task 5's and Task 6's `@NO_CASCADE_FAULT_YET`-marked tests in `tests/test_cascade.py` from `xfail` to real, passing assertions.

**Task 8 has landed**: a deterministic, template-driven simulated tier-2, so escalating is no longer a free pass. `session/ticket.py` gained `TicketState.AWAITING_TIER2`, `Ticket.escalation_note`/`tier2_bounces`, and `escalate()`/`reopen()`/`accept_escalation()` — escalating moves a ticket to `AWAITING_TIER2` without closing it, `reopen()` sends it back to `IN_PROGRESS` with `disposition` cleared and a `tier2` `ChatTurn` appended, and `accept_escalation()` is a thin wrapper over the existing `close(Disposition.ESCALATED, ...)`. `persona/models.py:ChatTurn.speaker` widened to `Literal["tech", "user", "tier2"]` — a third, distinct voice, not the reporting user. `session/tier2.py:review_escalation(ticket, fault, world) -> Tier2Response` judges ownership before evidence: a fixable fault (`escalation_is_correct = False`) is bounced regardless of how well-evidenced the note is, because it never belonged to tier-2 at all; only a fault that genuinely belongs there goes on to the evidence check, which requires the note to name something concrete — the placement target or the bound target of any `escalation_evidence` query (falling back to `diagnostic_path()` when a fault declares none, via `FaultBase`'s default `[]`) — and reject "not my problem"-style notes even when escalating was right. The fixable-fault bounce text is deliberately generic ("within your scope... the diagnostics you already have") rather than naming the fault's own diagnostic query, since any mention of AD-specific vocabulary risks colliding with that fault's own `leak_terms`. `ad.offboarded_reactivation` and `endpoint.failing_disk` each gained a populated `escalation_reason` (previously the `FaultBase` default empty string), which the accepted-response text quotes back to the technician — that's a system message to the technician, not persona output, so it's exempt from leak-term scrubbing on purpose. `grading.py:Grade.escalation_quality` (`"none"`/`"accepted"`/`"bounced"`, derived from `ticket.disposition`/`tier2_bounces`) never touches `correct` — a ticket bounced and then correctly fixed anyway still grades correct; only the after-action (Task 9) says the escalation itself was wrong. `SessionQueue.open_for(fault, at)` opens a ticket for a named fault at a named placement directly, bypassing the scheduler; `open_cascade(fault)` is now a one-line call to it with the fault's first placement. Verified end to end with a throwaway script exercising both paths against the real fault catalog: a fixable fault escalated, bounced, and fixed anyway (`escalation_quality="bounced"`, `correct=True`), and an escalate-only fault escalated with a well-evidenced note and accepted (`escalation_quality="accepted"`, `correct=True`).

`tests/conftest.py` clears the three `VITSC_*` variables for every test. Now that `AppSession.build` reads the environment, a developer with `VITSC_PERSONA=lmstudio` exported would otherwise point the entire suite at a local model; the Global Constraint that the suite passes with nothing on localhost is enforced there rather than left to habit.

## Commands

```bash
uv sync                                    # install deps (Python 3.12+, uv required)
uv run pytest                              # run the full suite
uv run pytest tests/test_catalog.py        # just the fault conformance harness
uv run pytest -k account_locked            # run tests matching a name/id
uv run pytest "tests/test_catalog.py::test_fault_conforms[ad.account_locked@m.alvarez]"  # one case

uv run pylint src                          # lint the package (CI runs this)
uv run pylint tests \
  --disable=redefined-outer-name,unused-variable,protected-access,use-implicit-booleaness-not-comparison
```

No model or network access is needed to run the suite.

Lint runs as two commands so `src/` keeps the stricter rule set; the four
disabled checks are pytest idioms that only ever fire in tests (a fixture
argument shadowing its fixture, unused halves of an unpacked setup tuple,
asserting on internals, and `== []` where the empty list is the point). Every
other disabled check, with its reason, is in `[tool.pylint]` in
`pyproject.toml`; a handful of narrow `# pylint: disable` comments sit inline
where pylint cannot infer the code (the `getattr` dispatch in
`env/simulated.py`, pydantic's `default_factory` in `tools/base.py`). Prefer
fixing a finding or suppressing it at its own line over adding to the global
list.

## Core design principle

**Tools read world state. They never read the fault.** A fault mutates the `World`; tools only ever call `env.read()` / `env.execute()`; a resolution mutates the world back; a fault's own `is_present()` is what decides whether it's fixed. Nothing anywhere branches on "if fault X is active." This is what makes multiple fix paths valid, lets harmless distractors be seeded honestly, and lets a future real-Windows-backed `Environment` slot in without touching tools or faults.

This is enforced mechanically, not just by convention: `tests/test_architecture.py` asserts nothing under `src/vitsc/tools/` imports from `vitsc.faults` or `vitsc.world` (tools must go through `Environment`'s `Query`/`Action`/`Observation` types only).

**Gotcha:** the conformance harness constructs `SimulatedEnvironment(broken)` *after* calling `fault.apply()`, never before. Any per-machine value an `Environment` caches at `__init__` time from mutable `World` state will see the already-faulted value, not the original — this bit `machine.renew_dhcp` (fixed by giving `Machine` a separate `dhcp_reserved_ip` field that faults never touch, distinct from the mutable `ip`). When adding a fault that nulls/clears a field a resolution needs to restore, give the restorable value its own fault-immune field rather than deriving it at Environment construction.

## Architecture

Layered, each layer written once against the layer below's protocol:

```
tools (ad, ps, net, eventlog, printing, remote)
  │  env.read(Query) / env.execute(Action)
Environment protocol  ← the swap point (vitsc/env/base.py)
  │
SimulatedEnvironment  ← v1 backend, in-memory World (vitsc/env/simulated.py)
  │  reads/writes self.world only
World (pydantic models: Organization, Machine, Printer, Share, Network)  ← vitsc/world/models.py
  ▲
fault catalog (vitsc/faults/catalog/*)  ← applies mutations to World via a Placement
```

- **`vitsc.world`** — pydantic entities (`World`, `ADUser`, `Machine`, `Printer`, `Share`, `Network`), the `company.yaml` seed loader (`world/seed.py`, always returns a healthy world — faults are what break it), and invariant checking (`world/invariants.py`: a `Baseline` captured *after* faults are applied, so only *collateral* damage from a bad fix trips it, never the fault itself being present).
- **`vitsc.env`** — the `Environment` protocol (`read`/`execute`/`snapshot`/`restore`) and `SimulatedEnvironment`, which dispatches `Query.kind`/`Action.kind` strings (e.g. `"ad.user"`, `"machine.renew_dhcp"`) to `_read_*`/`_do_*` methods by name via `getattr`. Adding a new query/action kind means adding a matching `_read_<kind>`/`_do_<kind>` method (dots become underscores).
- **`vitsc.faults`** — the `Fault` protocol (`vitsc/faults/base.py`): `placements()` finds valid targets in a `World`, `apply()` mutates it, `is_present()` is the single source of truth for both "is it broken" and "was it fixed", `symptoms()` returns only what a non-technical user could perceive (must contain no leaked jargon — see below), `diagnostic_path()` declares which queries should reveal it, `canonical_resolutions()` is documentation/test fixture only, never the pass/fail gate. New faults self-register via `register()` in `vitsc/faults/registry.py` at import time; `catalog/__init__.py` is what triggers that registration. A fault's `leak_terms` are matched by `persona.client.scrub()` anchored at a word *start*, so a term catches its own inflections (`"lock"` → "locking") without firing on words that merely contain it (`"lease"` does not match "please"); write a term with a trailing space (`"ad "`) to mean the whole word only.
- **`vitsc.tools`** — thin `DispatchTool` subclasses (`vitsc/tools/base.py`) that map command names to read/write `Query`/`Action` kinds and render output in the shape of the real utility (e.g. `Get-ADUser`-style field dumps, `ipconfig`-style block). Every call is logged to a `ToolLog`, which is what `session/grading.py` reads for tool efficiency and whether the technician looked before touching — so `mutating` on a `ToolCall` must stay honest (a rejected call that never reached the environment is not a mutation). `PowerShellConsole` is a *defined* command set, not a parser — anything outside it returns the same `CommandNotFoundException` text the real shell would, deliberately, rather than faking a general shell.
- **`vitsc.persona`** — the `Persona` protocol (`persona/models.py`) plus `PersonaCard` (built from an `ADUser` by `persona/personas.py:card_for`, seeded off `sam` so a person's mood/literacy is stable across sessions) and `TemplatePersona` (`persona/templates.py`), the model-free fallback. A persona's signatures take only a card and a `UserSymptoms` — never a `World` or a `Fault` — which is leak-prevention layer 1, enforced structurally. `TemplatePersona` keyword-matches the technician's question onto a symptom field and deflects everything else; it is what the whole test suite uses, so nothing downstream can depend on a model being loaded. `LMStudioPersona` (`persona/client.py`, constructed from `persona/config.py` when `VITSC_PERSONA=lmstudio`) talks to an OpenAI-compatible endpoint on `localhost:1234` and adds leak-prevention layers 2 (`persona/prompts.py` forbids guessing a cause) and 3 (`scrub()` against the fault's `leak_terms`, one retry with a nudge, then deflect). Every failure path — model down, slow, absent, or leaking twice — returns `TemplatePersona` output and sets `degraded`; the drill stays fully playable with nothing running locally, and `openai` is imported lazily inside `make_client()` so the suite never touches it.
- **`vitsc.session`** — `session/ticket.py` holds `Ticket` (the whole record of one job: persona, symptoms, chat, tool calls, actions, disposition), `Priority`/`SLA_MINUTES`, and `priority_for(fault, user)`, the system's triage call that the player's `user_priority` is graded against. This is the first layer that knows a fault *id*, but only as an opaque string — grading still asks `is_present()` against the world rather than trusting the ticket. `Ticket.close()` takes an explicit `at`, because SLA timing runs on the simulated `world.clock`, never wall time. `session/queue.py`'s `SessionQueue` is the scheduler: it picks an unheld, not-already-present fault+placement at random, applies it, and opens a ticket whose text came from the persona. Its `baseline` **accumulates** rather than re-snapshotting — `forgive()` excuses only the delta the newly applied fault made, so a technician's collateral damage from an earlier ticket stays visible for the rest of the session instead of being laundered by the next arrival. `session/grading.py` and `session/afteraction.py` close the loop: `grade_ticket()` asks `is_present()` and `check_invariants()` (never "did they use the canonical fix"), and `build_after_action()` names the root cause the player was never told, binds the fault's `diagnostic_path` into a shortest path, and picks a verdict from what the technician *did* — `Disposition` and `fault_cleared` separately, not from `disposition_correct`, which folds both together.

**Two clocks, deliberately.** `Ticket.opened_at` / `closed_at` and `world.clock` are simulated time and drive SLA. `ToolCall.at` and `ChatTurn.at` are wall clock (`datetime.now(UTC)`, timezone-aware) and exist only to order chat against tool calls for `questions_before_first_mutation`. Do not set `ChatTurn.at` from `world.clock` — mixing naive and aware datetimes raises at comparison time, and the two measure different things.

## The conformance harness

`tests/test_catalog.py` is parametrized over every registered fault × every placement it declares (`fault.id@placement.key`). For each case it mechanically proves: absent before `apply()` and present after; the declared `diagnostic_path` actually surfaces something and actually differs from the clean world; every `canonical_resolutions()` path drives `is_present()` false with zero invariant violations; and `symptoms()` contains none of the fault's own `leak_terms` and none of the shared `JARGON` set (dns, dhcp, active directory, lockout, etc — the player is meant to diagnose the mechanism, not read it off the ticket). A new fault gets full coverage from this harness with no new test written — just register it correctly.

When adding a fault, `docs/superpowers/plans/2026-08-07-phase-1-drill.md` has the task-by-task detail for the remaining catalog entries; `catalog/identity.py`'s `AccountLocked` is the reference example for the shape (placements/apply/is_present/symptoms/diagnostic_path/canonical_resolutions).

## Where the code deliberately diverges from the plan

The plan contains full code listings. Tasks 10–14 were implemented from them, but the following places are **intentionally different** and should not be "corrected" back to the plan's text. Each was verified against the real catalog before changing.

| Where | Plan said | Why the code differs |
| --- | --- | --- |
| `persona/client.py:scrub` | substring match on `leak_terms` | `"lease" in "please"` and `"ad " in "bad "` — a plain-English reply got discarded as a leak. Now anchored at a word start, with a trailing space meaning whole-word. |
| `persona/templates.py:_quoted_error` | always wrap as `It says "…"` | `ad.account_locked`'s `error_text` already opens with "It says", so it double-prefixed. Never strips text, so the raw `error_text` stays a substring of what is spoken. |
| `session/ticket.py:WORK_STOPPING` | omitted `ad.offboarded_reactivation` | It is a can't-sign-in fault; priority tracks impact on the person, not who ends up fixing it. |
| `world/invariants.py:capture_baseline` | `allowed_dns` from `network.dns_servers` | Broke capture-after-apply for `net.static_dns_misconfig`, which reported itself as the technician's collateral damage. Now snapshots machine state like every other field. |
| `session/queue.py` baseline | re-snapshot on every arrival | Laundered the technician's mistakes: damage done on ticket 1 vanished when ticket 2 arrived. `forgive()` accumulates instead. |
| `session/afteraction.py` verdict | branched on `disposition_correct` | That flag folds in `fault_cleared`, so closing-without-fixing was reported as a wrong escalation, and the last branch was unreachable. Branches on the chosen `Disposition` now. |
| `session/afteraction.py:touched_before_asking` | `questions == 0` | True whenever nobody mutated anything, so a correct read-only escalation was accused. Requires an actual mutation. |
| `grading.py:questions_before_first_mutation` | count all tech turns if the first call was a read | Counted questions asked *after* the fix as diligence. `ChatTurn` gained an `at` stamp so the two logs interleave. |
| `web/templates/layout.html` | render an org display name from `World` | `Organization` has no `name` field, only `domain: meridian.local` — "Meridian Freight Co." is hardcoded static branding text, not derived from world data. |
| `web/routes/tools.py:run_tool` | build a `ToolCall` for the unknown-tool branch and return it | The plan's version never calls `log.record(call)` on that path, so the failed call never reaches `log.calls` and never renders — `_toolout.html` would silently show nothing instead of the "not recognized" message. Now explicitly recorded, same as every call `DispatchTool.invoke` handles. |
| `web/templates/_queue.html` | Task 16 prose: show "a truncated opening line" | Task 19's `test_a_full_ticket_can_be_worked_through_http` asserts `ticket.report_text in client.get("/").text` — the two tasks' own descriptions conflict. The queue row renders the full `report_text`, not a 60-char slice of `symptoms.opening`. |
| `web/routes/events.py` | advance `world.clock` inline in the per-connection `stream()` loop | A first pass did this directly, but every open SSE connection (a second browser tab, an `EventSource` reconnect) runs its own copy of that loop against the same shared clock, so N connections made time run N times too fast. `AppSession.last_tick_at` plus `advance_clock_if_due()` now guard the increment so it happens at most once per real second regardless of connection count — caught by automated PR review, not the original test suite (see `tests/test_web_events.py`). |
| `web/templates/_ticket.html` / `_queue.html`: `report_text` | mark `report_text \| safe` to preserve embedded quotes | Rendering unescaped output was an XSS opening: `AppSession.build()` accepts any `Persona`, and a future model-backed one (`LMStudioPersona` already exists) could return markup that `\| safe` would let the browser execute. `report_text` is now autoescaped like everything else; tests compare against `markupsafe.escape(text)` instead of the raw string. Caught by automated PR review. |
| `tests/test_end_to_end.py` | resolve each fault via `session.env.execute()` directly | The only real HTTP tool call was a fixed `ad get-user` read, so the test's own claim — "proof that a full ticket can be worked start to finish through the HTTP surface only" — didn't hold for the repair step. `resolve_via_http()` now posts the fault's canonical fix through `POST /ticket/{id}/tool` via a `HTTP_FIX` command table, with a second test asserting every non-escalation fault has a table entry. Caught by automated PR review. |
| `tests/test_persona_binding.py`: leak-terms-in-prompt check | assert each leak term is absent from the prompt as a raw substring | The prompt legitimately carries the fault's own `error_text`, and `ad.account_locked`'s ends "contact my system administrator" — the `"ad "` term matched inside "administrator", failing a prompt that leaks nothing. Now asserts `scrub(prompt, leak_terms) is not None`: the same word-start-anchored rule layer 3 applies, and the one the conformance harness already proves symptom text satisfies. |
| `persona/client.py`: `LMStudioPersona.degraded` | `for_fault` returns "a new instance sharing the client, model and fallback"; `degraded` stays a plain instance attribute | After Task 1 every model call goes through a *binding*, never the persona `SessionQueue` holds — so a per-instance flag records an outage on an object nothing reads, and Task 2's banner (`AppSession.degraded` → `queue.persona.degraded`) would stay dark through one. The flag is now a `_Degradation` holder passed to each binding by reference, so degradation travels back to the origin while leak terms stay per-ticket. |
| `persona/client.py:make_client` | `OpenAI(base_url=..., api_key=...)` with no transport settings | The module docstring promises a model that is "down, slow, or absent" degrades gracefully, but the openai SDK's default timeout is 600s — a wedged LM Studio would hang the page for ten minutes rather than fall back. `make_client` now sets `REQUEST_TIMEOUT_SECONDS` (30s) and `TRANSPORT_RETRIES` (0): the persona layer's fallback *is* the retry strategy, so retrying underneath it only delays the inevitable. |
| `web/templates/layout.html`: degraded banner | "render a banner in `layout.html` when it is true" | Rendered at page load only, the banner would almost never appear: degradation starts mid-session, and in an HTMX app the next full page load may never come. The element is always rendered (`hidden` when healthy) and the existing SSE handler in `base.html` toggles it, so `events.py`'s payload carries `degraded` too. |
| `web/routes/events.py`: `build_payload` | payload dict built inline in the `stream()` loop | Asserting on the new `degraded` field meant driving `/events` through `TestClient`, and the stream loop never terminates — the test hung rather than failed. The payload builder is now a module-level function, matching how `advance_clock_if_due` was already split out of the same loop for the same reason, and `tests/test_web_events.py`'s existing habit of testing that route's pieces directly. |
| `env/simulated.py:_read_share_access` | plan silent on this; not in Task 4's stated file list | Task 4's `drive.stale_mapping` distractor is the first thing ever to put a value in `machine.mapped_drives` that isn't a key in `world.shares` (the point of a *stale* mapping). The old code indexed `world.shares[...]` directly, which would raise `KeyError` — an unhandled 500 — the moment a player ran `Get-PSDrive -Name Z:` against a seeded machine, since `PowerShellConsole.target_key` passes whatever drive letter the player types straight through. Now a missing share renders the same "network path was not found" text the DNS-failure branch already uses, which is also what real Windows shows for a mapping whose target no longer resolves. |
| `tests/test_catalog.py:test_v1_catalog_is_complete` | plan silent on this; not in Task 7's stated file list | The name predates Phase 2a and enumerates a fixed 10-fault set. `print.server_spooler_stopped` registers like any other fault and is picked up by the parametrized conformance tests automatically, but this one test hardcodes the whole id set and would otherwise fail the moment an 11th fault exists — it now includes the new id, with a comment noting the name is historical rather than a hard boundary. |
| `tests/test_grading.py:test_every_fault_produces_a_report_with_a_bound_shortest_path` | plan silent on this; not in Task 7's stated file list | Predates cascades: it derived the ticket's `sam` from `placement.kind == "user"` else `assigned_to`, which is `None` for a print server placement and raised `KeyError: None` indexing `world.org.users`. Now uses `session/queue.py:resolved_reporters(world, fault, placement)[0]`, the same resolution the scheduler itself uses, which covers both the single-reporter case and a cascade fault's own `reporters()`. |
| `session/tier2.py:review_escalation` | "Tier-2 judges two things, in this order: 1. Is the note usable? ... 2. Does this belong to tier-2 at all?" | The plan's own worked examples contradict that stated order: `test_a_fixable_fault_is_bounced_back` escalates `ad.account_locked` with a note that names no evidence at all ("please fix") and still expects the *scope-boundary* bounce text, not the *evidence-missing* one. Checking ownership (`escalation_is_correct`) first reconciles every example: a fixable fault is bounced regardless of note quality, and the evidence check only ever gates the accept path for a fault that genuinely belongs to tier-2. |

## Placement sentinels

`Fault.diagnostic_path()` and `canonical_resolutions()` receive only a `Placement` (kind + key), never a `World` — faults stay pure data, not closures over world state. When a query/action target needs something only `World` can resolve (a user's assigned hostname, a printer's parent machine, a share's required group), the fault embeds a sentinel string (`PLACEHOLDER`, `PLACEHOLDER_MACHINE`, `PLACEHOLDER_GROUP`, `PLACEHOLDER_PRINTER` in `vitsc/faults/base.py`) and the caller resolves it with `bind()` / `bind_query()` once `World` is in scope. The callers today are `tests/test_catalog.py` and `session/afteraction.py` (which binds `diagnostic_path()` into the report's `shortest_path`). `tests/test_grading.py` asserts every fault's report comes out sentinel-free, so a new fault that forgets to bind fails there.
